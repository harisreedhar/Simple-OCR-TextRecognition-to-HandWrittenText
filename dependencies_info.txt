tesseract can be installed on ubuntu using following command:
    sudo apt-get install tesseract-ocr
    sudo apt-get install tesseract-ocr-eng //for english trained data

Install pytesseract and pillow:
    pip install pytesseract
    pip install pillow
